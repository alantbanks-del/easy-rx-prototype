# Netlify config for ROE Easy-Rx (static single-file app + one serverless function)
[build]
  publish = "."
  command = ""
  functions = "netlify/functions"

[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
